{
  "name": "convtools",
  "version": "0.1.0",
  "description": "Conversations Tools",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "ajv": "^6.10.1",
    "del": "^5.0.0",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-nunjucks-render": "^2.2.3",
    "gulp-render-nunjucks": "^2.0.1",
    "gulp-sass": "^4.0.2",
    "gulp-sftp-clean": "^0.1.7",
    "nunjucks": "^3.2.0",
    "webpack": "^4.35.3",
    "webpack-cli": "^3.3.5"
  },
  "scripts": {
    "build": "gulp --gulpfile tasks build",
    "clean": "gulp --gulpfile tasks clean",
    "static": "gulp --gulpfile tasks static",
    "js": "gulp --gulpfile tasks js",
    "style": "gulp --gulpfile tasks style",
    "templates": "gulp --gulpfile tasks templates",
    "watch": "gulp --gulpfile tasks watch",
    "deploy": "gulp --gulpfile tasks deploy",
    "deploy-check": "gulp --gulpfile tasks deploy-check"
  }
}
